# 个人办公工作台后端 → 腾讯云 CloudBase 云托管 迁移清单

目标：把 sync-server（跨设备同步 + 客户资讯）从 Railway 迁到 CloudBase 云托管，
并锁定「最小实例数=0 → 缩容到 0 → 几元甚至接近 0 元/月」的省钱模式。

---

## 一、前置：开通与准备
1. 打开 https://console.cloud.tencent.com/cloudbase （用你腾讯云账号，即 wsa 那个账号）。
2. 新建一个「云开发环境」（环境名随意，如 `office-sync`），套餐选 **按量计费 / 免费体验版** 即可。
3. 进入该环境 → 左侧「云托管」→ 开通云托管。

## 二、创建服务（容器模式）
1. 云托管 → 服务管理 → 新建服务，服务名 `sync-server`，部署模式选「容器模式」。
2. 代码来源：选「代码仓库」（绑定 GitHub 的 Caydendesk/sync-server）或「本地代码/镜像」。
   - 用 GitHub 绑定最省事：之后 push 到 main 自动构建新版本（同 Railway 体验）。
   - 仓库根目录的 Dockerfile 会被自动识别。

## 三、部署版本配置（关键省钱项）
新建版本时填写：
- **启动命令**：`node server.js`（Dockerfile 已兜底，留空也可）
- **监听端口**：`3000`
- **环境变量**（逐个添加，密钥绝不写死进代码）：
  - `PORT` = `3000`
  - `TENCENT_WSA_API_KEY` = `sk-0c8224bf06b4b5c3de38952b8912c4906296`
  - `TAVILY_API_KEY` = （如有，填值；无则跳过）
  - `EXA_API_KEY` = （如有，填值；无则跳过）
  - `DATA_DIR` = `/data`   ← 必填，指向下面的持久卷挂载点
- **实例副本数**：**最小 0 / 最大 1**（最小设为 0 才能缩容到 0，达成 0 成本）
- **扩缩容条件**：默认 CPU≥60% 即可，个人用触发不到
- **健康检查**：`/health`（代码已支持）

## 四、挂持久卷（治“部署即丢数据”的命门）
⚠️ 云托管容器文件系统**不持久**，不挂卷数据必丢。
1. 云托管 → 服务设置 → 存储 / 持久化存储 → 新建 CFS 持久卷（按量，个人量级几毛钱/月）。
2. 把卷挂载到容器路径：**`/data`**（与上面 `DATA_DIR=/data` 对应）。
3. 保存后重新部署一次版本，让挂载生效。

## 五、拿到访问域名
部署完成后，服务会分配一个公网 HTTPS 域名，形如：
`https://sync-server-xxxx.ap-shanghai.run.tcloudbase.com`
记下它，下一步填进前端。

## 六、前端改同步地址
打开「个人办公工作台.html」→ 左上角「空间」芯片旁的同步设置（DEFAULT_SYNC_URL），
把地址改成上面那个 CloudBase 域名（带 https）。重新混淆部署到 CloudStudio。

## 七、数据迁移（一次性）
当前 Railway 上若有 Cayden/Cayde/Cayd 三个桶的真实数据，需要先迁过来：
1. 从主设备（笔记本）打开工作台，点「同步到云端」确保本地有最新数据；
2. 因为新 CloudBase 后端是空卷，首次改完地址后，前端会“上传本地数据”写进新桶；
3. 之后跨设备都连 CloudBase，数据就在 `/data/sync-data.json` 持久保存。

## 八、缩容到 0 的验证
部署后连续 30 分钟无请求，到「服务管理」看实例数是否变 0。
再次访问时会有几秒冷启动延迟（个人用可接受）。确认实例为 0 即代表当月资源费趋近 0。

## 九、费用预期（个人量级）
- 实例 0 时常驻费 = 0
- 仅出网流量 + 偶发冷启动实例费 → 几元/月甚至接近 0
- CFS 卷存储费：几 MB 数据 ≈ 忽略不计
- 总体：远低于 Railway 的 $5/月，且国内访问快、合规

## 十、Railway 处理
迁移验证无误后，可在 Railway 控制台删除旧服务（避免继续产生 $5/月），
或保留作备份（建议先留 1 个月观察）。

---
附：代码侧已完成的改造（本仓库）
- server.js：`DATA_FILE` 支持 `DATA_DIR` 环境变量；`saveData` 自动建目录；资讯加 20 分钟内存缓存
- Dockerfile：云托管容器构建
- railway.json：探活配置（过渡期 Railway 仍可用）
